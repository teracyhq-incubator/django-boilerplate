This is the directory for project Django applications.

This directory is added to the Python system path.

To start a new application: `$ mkdir apps/<app_name>  && python manage.py startapp <app_name> apps/<app_name>`.
